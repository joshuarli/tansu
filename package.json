{
  "name": "tansu",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "for f in web/ts/*.test.ts; do bun run \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "happy-dom": "^20.8.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
