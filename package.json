{
  "name": "tansu",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "for f in web/ts/*.test.ts; do bun run \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "happy-dom": "^20.8.9",
    "oxfmt": "0.43.0",
    "oxlint": "1.58.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
